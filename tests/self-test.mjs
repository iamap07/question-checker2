import assert from 'node:assert/strict'; const {parseGoogleSheetUrl}=await import('../lib/google/url.ts'); const {normalizeQuestion,extractNumericFeatures,splitPageIntoQuestionBlocks}=await import('../lib/pdf/questions.ts'); const {canonicalPair,scorePair,classify}=await import('../lib/similarity/engine.ts'); assert.deepEqual(parseGoogleSheetUrl('https://docs.google.com/spreadsheets/d/abc/edit?gid=42#gid=42'),{spreadsheetId:'abc',gid:'42'}); assert.equal(normalizeQuestion('Q12 Find 70% of 200'),'find 70% of 200'); const n=extractNumericFeatures('A train moves at 108 km/h for 15 sec, ratio 3:2 and profit 25%.'); assert(n.numbers.includes(108)&&n.percentages.includes(25)&&n.ratios.includes('3:2')&&n.units.includes('km/h')); assert.equal(splitPageIntoQuestionBlocks(1,'Q1 A train travels 120 km in 2 hours. Find its speed. Q2 What is the perimeter of a square having area 361 cm²?').length,2); assert.deepEqual(canonicalPair('b','a'),['a','b']); const base={documentId:'a',pdfName:'a',pdfUrl:'',questionNumber:'1',pageNumber:1,rawQuestionText:'',options:[],subject:'arithmetic',questionType:'arithmetic',numericFeatures:{numbers:[120,2],percentages:[],ratios:[],units:[],variables:[],equations:[],operators:[]}};let a={...base,questionText:'find 70% of 200',numericFeatures:{...base.numericFeatures,numbers:[70,200]}};let b={...base,documentId:'b',questionText:'find 70% of 200',numericFeatures:{...base.numericFeatures,numbers:[70,200]}};assert.equal(classify(scorePair(a,b,1),a,b).category,'EXACT_DUPLICATE'); a={...base,questionText:'a train travels 120 km in 2 hours find speed'}; b={...base,documentId:'b',questionText:'a train travels 240 km in 4 hours find speed',numericFeatures:{...base.numericFeatures,numbers:[240,4]}};assert.equal(classify(scorePair(a,b,.9),a,b).category,'SAME_STRUCTURE_DIFFERENT_VALUES'); console.log('SELF-TEST PASS');
