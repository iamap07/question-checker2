"use client"; import {signIn,signOut,useSession} from 'next-auth/react'; import {Button} from './ui'; export function Header(){const {data}=useSession();return <header className="sticky top-0 z-20 flex items-center justify-between border-b border-slate-200 bg-white/85 px-5 py-4 backdrop-blur md:ml-64"><div><div className="text-sm text-slate-500">Question Paper Similarity Checker</div><div className="font-bold">Audit your question bank with confidence</div></div>{data?.user?<Button className="bg-slate-900" onClick={()=>signOut({callbackUrl:'/'})}>Sign out</Button>:<Button onClick={()=>signIn('google')}>Connect Google</Button>}</header>}
