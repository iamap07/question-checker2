import dns from 'node:dns/promises'; import net from 'node:net';
const privateIp=(ip:string)=>net.isIPv4(ip)?(()=>{const [a,b]=ip.split('.').map(Number);return a===10||a===127||(a===169&&b===254)||(a===172&&b>=16&&b<=31)||(a===192&&b===168)})():ip==='::1'||ip.startsWith('fc')||ip.startsWith('fd')||ip.startsWith('fe80:');
export async function assertSafeExternalUrl(raw:string){const u=new URL(raw);if(!['http:','https:'].includes(u.protocol))throw new Error('Only HTTP/HTTPS PDF URLs are supported');if(['localhost','127.0.0.1','0.0.0.0','::1'].includes(u.hostname))throw new Error('Private network hosts are not allowed');const resolved=await dns.lookup(u.hostname,{all:true});if(resolved.some(x=>privateIp(x.address)))throw new Error('Private network targets are not allowed');return u}
