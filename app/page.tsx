"use client"; import {useSession} from 'next-auth/react'; import {useRouter} from 'next/navigation'; import {FormEvent,useState} from 'react'; import {Button,Card,Badge} from '@/components/ui'; import {LinkIcon,ArrowRight,CheckCircle2,LockKeyhole,BarChart3} from 'lucide-react';
export default function Home(){const {data}=useSession();const router=useRouter();const[url,setUrl]=useState('');const[busy,setBusy]=useState(false);const[error,setError]=useState('');async function start(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/scans',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({sheetUrl:url})});const j=await r.json();if(!r.ok)throw new Error(j.error);router.push(`/scans/${j.id}${j.token?`?token=${encodeURIComponent(j.token)}`:''}`)}catch(e){setError(e instanceof Error?e.message:String(e))}finally{setBusy(false)}}return <div className="min-h-screen"><div className="mx-auto grid max-w-6xl gap-10 px-6 py-12 md:grid-cols-[1.1fr_.9fr] md:py-20"><div className="pt-8"><Badge tone="blue">Production workflow · Google + AI</Badge><h1 className="mt-5 text-5xl font-black tracking-tight">Find repeated questions before they reach the next paper.</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-slate-600">Paste a Google Sheet URL. The checker resolves PDF links, parses question sections, uses OCR when needed, compares mathematical structure and semantics, and gives you an auditable conflict report.</p><div className="mt-8 flex flex-wrap gap-6 text-sm text-slate-600"><div className="flex items-center gap-2"><CheckCircle2 size={18}/>Exact + near duplicate detection</div><div className="flex items-center gap-2"><BarChart3 size={18}/>Hybrid similarity scoring</div><div className="flex items-center gap-2"><LockKeyhole size={18}/>Server-side credentials</div></div></div><Card className="p-7"><div className="text-xl font-bold">Start a scan</div><p className="mt-2 text-sm text-slate-500">Column B is scanned for PDF links and Drive URLs.</p><form onSubmit={start} className="mt-6 space-y-4"><label className="text-sm font-semibold">Google Sheet URL<input className="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3 outline-none focus:border-[#3155d8]" value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://docs.google.com/spreadsheets/d/..." required/></label>{error&&<div className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</div>}<Button disabled={busy} className="w-full">{busy?'Creating scan…':<>Start Scan <ArrowRight size={17} className="ml-2"/></>}</Button></form><div className="mt-5 rounded-xl bg-slate-50 p-4 text-xs leading-5 text-slate-500"><LinkIcon size={14}/> Public sheets can be scanned without Google sign-in; private Sheets/Drive files require OAuth.</div></Card></div></div>}
